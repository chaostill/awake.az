/* ── Motion+ premium components (re-exported for tree-shaking) ── */
export {
	AnimateNumber,
	AnimateText,
	ScrambleText,
	Ticker,
	Typewriter,
} from "motion-plus/react"

/* ── Motion tokens ── */
export { springs, durations, ease, sectionEntrance, stagger } from "../../lib/motion-tokens"
